import Nav from '../Nav';

export default function Header()
{
    return(
        <section id="top-bar">
            <nav>
                <Nav />
            </nav>
            
        </section>
    );
}